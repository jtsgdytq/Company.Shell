<UserControl
    x:Class="Company.Application.Initialize.Views.IniteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:iconPack="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Company.Application.Initialize.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Company.Application.Initialize.ViewModels"
    d:DataContext="{d:DesignInstance vm:IniteViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{StaticResource Company.Login.Background}"
    mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding IniteLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="30"
                Foreground="White"
                Text="初始化硬件加载" />
            <mah:MetroProgressBar
                Width="250"
                Margin="4"
                VerticalAlignment="Center"
                Foreground="White"
                IsIndeterminate="True" />
            <StackPanel
                Margin="0,10"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <Button
                    Width="40"
                    Height="30"
                    Margin="10,5"
                    Command="{Binding LoadedCommand}"
                    Content="跳过" />
                <Button
                    Width="40"
                    Height="30"
                    Margin="10,5"
                    Command="{Binding ReloadedCommand}"
                    Content="重试" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
