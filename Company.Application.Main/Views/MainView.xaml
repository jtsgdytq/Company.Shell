<UserControl
    x:Class="Company.Application.Main.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Company.Application.Main.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Company.Application.Main.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="1500"
    Background="{StaticResource Company.Main.Background}"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <RadioButton
                    Content="开始"
                    IsChecked="True"
                    Style="{StaticResource RadioButton.Menu.Style}">
                    <RadioButton.Tag>
                        <iconPacks:PackIconBoxIcons
                            Width="20"
                            Height="20"
                            Kind="RegularHome" />
                    </RadioButton.Tag>
                </RadioButton>
                <RadioButton Style="{StaticResource RadioButton.Menu.Style}" Content="设置">
                    <RadioButton.Tag>
                        <iconPacks:PackIconFeatherIcons
                            Width="20"
                            Height="20"
                            Kind="Settings" />
                    </RadioButton.Tag>
                </RadioButton>
                <RadioButton Content="相机标定" Style="{StaticResource RadioButton.Menu.Style}">
                    
                </RadioButton>

                <RadioButton Content="Mark校准" Style="{StaticResource RadioButton.Menu.Style}">

                </RadioButton>
                <RadioButton Content="轴校准" Style="{StaticResource RadioButton.Menu.Style}">

                </RadioButton>
                <RadioButton Content="BOM管理" Style="{StaticResource RadioButton.Menu.Style}">

                </RadioButton>
                <RadioButton Content="用户切换" Style="{StaticResource RadioButton.Menu.Style}">

                </RadioButton>
            </StackPanel>
        </Grid>
        <Grid Background="Black" Visibility="Hidden" />
    </Grid>
</UserControl>
