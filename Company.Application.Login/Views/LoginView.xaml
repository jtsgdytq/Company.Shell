<UserControl
    x:Class="Company.Application.Login.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:Company.Application.Login.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vm="clr-namespace:Company.Application.Login.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{StaticResource Company.Login.Background}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBlock
                FontFamily="Arial"
                FontSize="60"
                FontWeight="Bold"
                Foreground="White"
                Text="视觉对位" />
            <TextBlock
                FontSize="40"
                Foreground="White"
                Text="运动控制系统" />
        </StackPanel>
        <Border
            Grid.Column="1"
            Width="2"
            Margin="0,50"
            Background="White" />
        <Grid
            Grid.Column="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <icon:PackIconModern
                Grid.Row="0"
                Grid.Column="0"
                Width="20"
                Height="20"
                Foreground="White"
                Kind="CameraAuto" />
            <StackPanel
                Grid.Row="1"
                Grid.Column="0"
                Orientation="Horizontal">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="账号:" />
                <TextBox
                    Width="100"
                    Height="15"
                    Margin="0,2"
                    Text="{Binding User.UserName, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel
                Grid.Row="2"
                Grid.Column="0"
                Orientation="Horizontal">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="密码:" />
                <TextBox
                    Width="100"
                    Height="15"
                    Margin="0,2"
                    Text="{Binding User.Password, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />
            </StackPanel>
            <Button
                Grid.Row="1"
                Grid.RowSpan="2"
                Grid.Column="1"
                Width="50"
                Height="50"
                Margin="2"
                Content="登录" Command="{Binding  LoginCommand}" />
        </Grid>
    </Grid>
</UserControl>
